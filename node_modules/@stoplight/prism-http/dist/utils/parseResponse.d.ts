import { Response } from 'node-fetch';
import * as TaskEither from 'fp-ts/lib/TaskEither';
import { Dictionary } from '@stoplight/types';
import { IHttpResponse } from '../types';
export declare function parseResponseBody(response: Pick<Response, 'headers' | 'json' | 'text'>): TaskEither.TaskEither<Error, unknown>;
export declare function parseResponseHeaders(response: Pick<Response, 'headers'>): Dictionary<string, string>;
export declare function parseResponse(response: Pick<Response, 'headers' | 'json' | 'text' | 'status'>): TaskEither.TaskEither<Error, IHttpResponse>;
