import { INodeVariable, IServer } from '@stoplight/types';
import { MatchType } from './types';
export declare function matchBaseUrl(server: IServer, baseUrl: string): MatchType;
export declare function convertTemplateToRegExp(urlTemplate: string, variables?: {
    [name: string]: INodeVariable;
}): RegExp;
