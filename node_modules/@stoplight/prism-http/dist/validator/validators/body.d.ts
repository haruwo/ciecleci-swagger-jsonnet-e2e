import { IPrismDiagnostic } from '@stoplight/prism-core';
import { Dictionary, IHttpEncoding, IMediaTypeContent } from '@stoplight/types';
import * as Either from 'fp-ts/lib/Either';
import * as Option from 'fp-ts/lib/Option';
import { JSONSchema } from '../../types';
import { IHttpValidator } from './types';
export declare function deserializeFormBody(schema: JSONSchema, encodings: IHttpEncoding[], decodedUriParams: Dictionary<string, string>): Dictionary<string, string>;
export declare function splitUriParams(target: string): Dictionary<string, string>;
export declare function decodeUriEntities(target: Dictionary<string, string>): {};
export declare function findContentByMediaTypeOrFirst(specs: IMediaTypeContent[], mediaType: string): Option.Option<{
    mediaType: string;
    content: IMediaTypeContent;
}>;
export declare class HttpBodyValidator implements IHttpValidator<any, IMediaTypeContent> {
    private prefix;
    constructor(prefix: string);
    validate(target: any, specs: IMediaTypeContent[], mediaType?: string): Either.Either<import("fp-ts/lib/NonEmptyArray").NonEmptyArray<IPrismDiagnostic>, any>;
}
