import { IPrismComponents } from '@stoplight/prism-core';
import { IHttpOperation } from '@stoplight/types';
import { IHttpRequest, IHttpResponse, IMockHttpConfig } from '../types';
declare const mock: IPrismComponents<IHttpOperation, IHttpRequest, IHttpResponse, IMockHttpConfig>['mock'];
export default mock;
